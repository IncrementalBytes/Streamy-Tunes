<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:layout_marginStart="2dp"
  android:layout_marginTop="2dp"
  android:layout_marginEnd="2dp"
  android:background="@color/background">

  <ImageView
    android:id="@+id/song_item_image_album"
    android:layout_width="@dimen/image_width_thumbnail"
    android:layout_height="@dimen/image_height_thumbnail"
    android:layout_margin="@dimen/default_view_margin"
    android:background="@color/transparent"
    android:src="@drawable/ic_song_dark"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintBottom_toBottomOf="parent"
    android:contentDescription="@string/song" />

  <TextView
    android:id="@+id/song_item_text_title"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/default_view_margin"
    android:ellipsize="end"
    android:text="@string/placeholder"
    android:textSize="14sp"
    android:textStyle="bold"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toEndOf="@id/song_item_image_album"
    app:layout_constraintTop_toTopOf="parent" />

  <TextView
    android:id="@+id/song_item_text_artist"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/default_view_margin"
    android:text="@string/placeholder"
    android:textColor="@android:color/holo_blue_light"
    android:textSize="14sp"
    android:textStyle="bold"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintStart_toEndOf="@id/song_item_image_album"
    app:layout_constraintTop_toBottomOf="@id/song_item_text_title" />

  <TextView
    android:id="@+id/song_item_text_album"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/default_view_margin"
    android:text="@string/placeholder"
    android:textStyle="bold"
    app:layout_constraintStart_toEndOf="@id/song_item_image_album"
    app:layout_constraintTop_toBottomOf="@id/song_item_text_artist"
    app:layout_constraintBottom_toBottomOf="parent"
    app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
